/**
 * Kowabunga API documentation
 * Kvm Orchestrator With A BUNch of Goods Added
 *
 * The version of the OpenAPI document: 0.52.5
 * Contact: maintainers@kowabunga.cloud
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  StorageNFS,
} from './models';

/**
 * deleteStorageNFS - parameters interface
 */
export interface IDeleteStorageNFSParams {
  nfsId: string;
}

/**
 * listStorageNFSKylos - parameters interface
 */
export interface IListStorageNFSKylosParams {
  nfsId: string;
}

/**
 * listStorageNFSs - parameters interface
 */
export interface IListStorageNFSsParams {
}

/**
 * readStorageNFS - parameters interface
 */
export interface IReadStorageNFSParams {
  nfsId: string;
}

/**
 * updateStorageNFS - parameters interface
 */
export interface IUpdateStorageNFSParams {
  nfsId: string;
  storageNFS: StorageNFS;
}

/**
 * NfsApi - API class
 */
@autoinject()
export class NfsApi extends Api {

  /**
   * Creates a new NfsApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Deletes an existing NFS storage.
   * @param params.nfsId The ID of the NFS storage.
   */
  async deleteStorageNFS(params: IDeleteStorageNFSParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('deleteStorageNFS', params, 'nfsId');

    // Create URL to call
    const url = `${this.basePath}/nfs/{nfsId}`
      .replace(`{${'nfsId'}}`, encodeURIComponent(`${params['nfsId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Authentication 'ApiKeyAuth' required
      .withHeader('X-API-Key', this.authStorage.getApiKeyAuth())
      // Authentication 'BearerAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Returns the IDs of Kylo objects.
   * @param params.nfsId The ID of the NFS storage.
   */
  async listStorageNFSKylos(params: IListStorageNFSKylosParams): Promise<Array<string>> {
    // Verify required parameters are set
    this.ensureParamIsSet('listStorageNFSKylos', params, 'nfsId');

    // Create URL to call
    const url = `${this.basePath}/nfs/{nfsId}/kylo`
      .replace(`{${'nfsId'}}`, encodeURIComponent(`${params['nfsId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'ApiKeyAuth' required
      .withHeader('X-API-Key', this.authStorage.getApiKeyAuth())
      // Authentication 'BearerAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Returns the IDs of NFS storage objects.
   */
  async listStorageNFSs(): Promise<Array<string>> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/nfs`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'ApiKeyAuth' required
      .withHeader('X-API-Key', this.authStorage.getApiKeyAuth())
      // Authentication 'BearerAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Returns a NFS storage.
   * @param params.nfsId The ID of the NFS storage.
   */
  async readStorageNFS(params: IReadStorageNFSParams): Promise<StorageNFS> {
    // Verify required parameters are set
    this.ensureParamIsSet('readStorageNFS', params, 'nfsId');

    // Create URL to call
    const url = `${this.basePath}/nfs/{nfsId}`
      .replace(`{${'nfsId'}}`, encodeURIComponent(`${params['nfsId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'ApiKeyAuth' required
      .withHeader('X-API-Key', this.authStorage.getApiKeyAuth())
      // Authentication 'BearerAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Updates a NFS storage configuration.
   * @param params.nfsId The ID of the NFS storage.
   * @param params.storageNFS StorageNFS payload.
   */
  async updateStorageNFS(params: IUpdateStorageNFSParams): Promise<StorageNFS> {
    // Verify required parameters are set
    this.ensureParamIsSet('updateStorageNFS', params, 'nfsId');
    this.ensureParamIsSet('updateStorageNFS', params, 'storageNFS');

    // Create URL to call
    const url = `${this.basePath}/nfs/{nfsId}`
      .replace(`{${'nfsId'}}`, encodeURIComponent(`${params['nfsId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPut()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['storageNFS'] || {}))

      // Authentication 'ApiKeyAuth' required
      .withHeader('X-API-Key', this.authStorage.getApiKeyAuth())
      // Authentication 'BearerAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

