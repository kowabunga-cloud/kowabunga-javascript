/**
 * Kowabunga API documentation
 * Kvm Orchestrator With A BUNch of Goods Added
 *
 * The version of the OpenAPI document: 0.52.5
 * Contact: maintainers@kowabunga.cloud
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  Team,
} from './models';

/**
 * createTeam - parameters interface
 */
export interface ICreateTeamParams {
  team: Team;
}

/**
 * deleteTeam - parameters interface
 */
export interface IDeleteTeamParams {
  teamId: string;
}

/**
 * listTeams - parameters interface
 */
export interface IListTeamsParams {
}

/**
 * readTeam - parameters interface
 */
export interface IReadTeamParams {
  teamId: string;
}

/**
 * updateTeam - parameters interface
 */
export interface IUpdateTeamParams {
  teamId: string;
  team: Team;
}

/**
 * TeamApi - API class
 */
@autoinject()
export class TeamApi extends Api {

  /**
   * Creates a new TeamApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Creates a new Kowabunga users team.
   * @param params.team Team payload.
   */
  async createTeam(params: ICreateTeamParams): Promise<Team> {
    // Verify required parameters are set
    this.ensureParamIsSet('createTeam', params, 'team');

    // Create URL to call
    const url = `${this.basePath}/team`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['team'] || {}))

      // Authentication 'ApiKeyAuth' required
      .withHeader('X-API-Key', this.authStorage.getApiKeyAuth())
      // Authentication 'BearerAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Deletes an existing Kowabunga users team.
   * @param params.teamId The ID of the Kowabunga users team.
   */
  async deleteTeam(params: IDeleteTeamParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('deleteTeam', params, 'teamId');

    // Create URL to call
    const url = `${this.basePath}/team/{teamId}`
      .replace(`{${'teamId'}}`, encodeURIComponent(`${params['teamId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Authentication 'ApiKeyAuth' required
      .withHeader('X-API-Key', this.authStorage.getApiKeyAuth())
      // Authentication 'BearerAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Returns the IDs of Kowabunga users team objects.
   */
  async listTeams(): Promise<Array<string>> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/team`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'ApiKeyAuth' required
      .withHeader('X-API-Key', this.authStorage.getApiKeyAuth())
      // Authentication 'BearerAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Returns a Kowabunga users team.
   * @param params.teamId The ID of the Kowabunga users team.
   */
  async readTeam(params: IReadTeamParams): Promise<Team> {
    // Verify required parameters are set
    this.ensureParamIsSet('readTeam', params, 'teamId');

    // Create URL to call
    const url = `${this.basePath}/team/{teamId}`
      .replace(`{${'teamId'}}`, encodeURIComponent(`${params['teamId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'ApiKeyAuth' required
      .withHeader('X-API-Key', this.authStorage.getApiKeyAuth())
      // Authentication 'BearerAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Updates a Kowabunga users team configuration.
   * @param params.teamId The ID of the Kowabunga users team.
   * @param params.team Team payload.
   */
  async updateTeam(params: IUpdateTeamParams): Promise<Team> {
    // Verify required parameters are set
    this.ensureParamIsSet('updateTeam', params, 'teamId');
    this.ensureParamIsSet('updateTeam', params, 'team');

    // Create URL to call
    const url = `${this.basePath}/team/{teamId}`
      .replace(`{${'teamId'}}`, encodeURIComponent(`${params['teamId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPut()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['team'] || {}))

      // Authentication 'ApiKeyAuth' required
      .withHeader('X-API-Key', this.authStorage.getApiKeyAuth())
      // Authentication 'BearerAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

