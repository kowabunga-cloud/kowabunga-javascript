/**
 * Kowabunga API documentation
 * Kvm Orchestrator With A BUNch of Goods Added
 *
 * The version of the OpenAPI document: 0.52.5
 * Contact: maintainers@kowabunga.cloud
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  Template,
} from './models';

/**
 * deleteTemplate - parameters interface
 */
export interface IDeleteTemplateParams {
  templateId: string;
}

/**
 * listTemplates - parameters interface
 */
export interface IListTemplatesParams {
}

/**
 * readTemplate - parameters interface
 */
export interface IReadTemplateParams {
  templateId: string;
}

/**
 * updateTemplate - parameters interface
 */
export interface IUpdateTemplateParams {
  templateId: string;
  template: Template;
}

/**
 * TemplateApi - API class
 */
@autoinject()
export class TemplateApi extends Api {

  /**
   * Creates a new TemplateApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Deletes an existing image template.
   * @param params.templateId The ID of the image template.
   */
  async deleteTemplate(params: IDeleteTemplateParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('deleteTemplate', params, 'templateId');

    // Create URL to call
    const url = `${this.basePath}/template/{templateId}`
      .replace(`{${'templateId'}}`, encodeURIComponent(`${params['templateId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Authentication 'ApiKeyAuth' required
      .withHeader('X-API-Key', this.authStorage.getApiKeyAuth())
      // Authentication 'BearerAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Returns the IDs of image template objects.
   */
  async listTemplates(): Promise<Array<string>> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/template`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'ApiKeyAuth' required
      .withHeader('X-API-Key', this.authStorage.getApiKeyAuth())
      // Authentication 'BearerAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Returns a image template.
   * @param params.templateId The ID of the image template.
   */
  async readTemplate(params: IReadTemplateParams): Promise<Template> {
    // Verify required parameters are set
    this.ensureParamIsSet('readTemplate', params, 'templateId');

    // Create URL to call
    const url = `${this.basePath}/template/{templateId}`
      .replace(`{${'templateId'}}`, encodeURIComponent(`${params['templateId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'ApiKeyAuth' required
      .withHeader('X-API-Key', this.authStorage.getApiKeyAuth())
      // Authentication 'BearerAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Updates a image template configuration.
   * @param params.templateId The ID of the image template.
   * @param params.template Template payload.
   */
  async updateTemplate(params: IUpdateTemplateParams): Promise<Template> {
    // Verify required parameters are set
    this.ensureParamIsSet('updateTemplate', params, 'templateId');
    this.ensureParamIsSet('updateTemplate', params, 'template');

    // Create URL to call
    const url = `${this.basePath}/template/{templateId}`
      .replace(`{${'templateId'}}`, encodeURIComponent(`${params['templateId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPut()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['template'] || {}))

      // Authentication 'ApiKeyAuth' required
      .withHeader('X-API-Key', this.authStorage.getApiKeyAuth())
      // Authentication 'BearerAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

